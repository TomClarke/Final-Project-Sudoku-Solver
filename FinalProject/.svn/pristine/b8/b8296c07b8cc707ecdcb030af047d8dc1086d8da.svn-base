/**
 * 
 */
package src;

import javax.swing.JButton;



public class Model {

		  
private final View view = new View();

	
	public void Solve() {
		
	}

	/**
	 * The implementation for event
	 * Save will use a new saved models and number placements 
	 * which could be stored in XML files 
	 */
	public void Save() {
		view.Save();
	}
	
	/**
	 * The implementation for event
	 * Load will use the old saved models and number placements 
	 * which could be stored in XML files 
	 */
	public void Load() {

		
		view.Load();
		
	}
	
	/**
	 * The implementation for event
	 * Erase will delete all of the current numbers within the buttons
	 * in the sudoku grid
	 */
	public void Erase() {

	}

	/**
	 * The implementation for event
	 * Help will show the users a helpful suggestion or fill in few numbers
	 * that could provide help for the user
	 */
	public void Help() {
		
	}
	
	
	/**
	 * The implementation for event
	 * Buttons are all the single buttons within the sudoku grid and 
	 * when selected that will allow the the user to edit those numbers 
	 */
	public void Buttons() {
		
	}

}